name: "Deploy NextJS VPS Hostinger"
on:
  push:
    branches:
      - main

concurrency:
  group: deploy-production
  cancel-in-progress: true

jobs:
  build_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy no servidor
        uses: appleboy/ssh-action@v0.1.7
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            set -euo pipefail

            APP_DIR=/root/projects/seligadev
            REPO_URL=https://github.com/jnerydesigner/seligadev-site.git

            mkdir -p /root/projects

            if [ ! -d "$APP_DIR/.git" ]; then
                echo "Clonando repositório..."
                git clone "$REPO_URL" "$APP_DIR"
            else
                echo "Repositório já clonado"
            fi

            echo "Entrando na pasta do projeto"
            cd "$APP_DIR"
            echo "PWD após cd:"
            pwd
            echo "Conteúdo da pasta do projeto:"
            ls -la

            echo "Atualizando código do repositório"
            git fetch origin main
            git reset --hard origin/main

            echo "Instalando dependências"
            yarn install --frozen-lockfile

            echo "Executando build"
            export DATABASE_URL=${{ secrets.DATABASE_URL }}
            export NEXT_PUBLIC_BASE_URL=${{ secrets.NEXT_PUBLIC_BASE_URL }}
            yarn build
            test -f .next/BUILD_ID

            echo "Reiniciando aplicação com PM2"
            pm2 start my-pm2-start.json --only seligadev --update-env || pm2 restart seligadev --update-env
            pm2 save

            echo "Executando health check"
            curl -f http://127.0.0.1:4455/ > /dev/null

      # - name: Enviar e-mail de sucesso
      #   if: success()
      #   uses: dawidd6/action-send-mail@v3
      #   with:
      #     server_address: smtp.gmail.com
      #     server_port: 465
      #     secure: true
      #     username: ${{ secrets.EMAIL_USER }}
      #     password: ${{ secrets.EMAIL_PASS }}
      #     subject: "✅ Deploy NextJS concluído com sucesso"
      #     to: jander.webmaster@gmail.com
      #     from: GitHub Actions <${{ secrets.EMAIL_USER }}>
      #     body: |
      #       <html>
      #         <body style="font-family: Arial, sans-serif; background-color: #f4f4f4; padding: 20px;">
      #           <div style="max-width: 600px; margin: auto; background-color: #fff; padding: 20px; border-radius: 10px; box-shadow: 0 2px 5px rgba(0,0,0,0.1);">
      #             <h2 style="color: #2F855A;">✅ Deploy Concluído com Sucesso!</h2>
      #             <p>Olá,</p>
      #             <p>O deploy do projeto <strong>NextJS</strong> no servidor <strong>Hostinger</strong> foi concluído com sucesso.</p>
      #             <p><strong>Detalhes do deploy:</strong></p>
      #             <ul>
      #               <li>Branch: <code>main</code></li>
      #               <li>Servidor: <code>${{ secrets.HOST }}</code></li>
      #               <li>Data/Hora: <strong>${{ github.event.head_commit.timestamp }}</strong></li>
      #             </ul>
      #             <p style="color: #2F855A; font-weight: bold;">Tudo está funcionando normalmente! 🚀</p>
      #             <hr>
      #             <p style="font-size: 12px; color: #888;">Este é um e-mail automático enviado pelo GitHub Actions.</p>
      #           </div>
      #         </body>
      #       </html>
